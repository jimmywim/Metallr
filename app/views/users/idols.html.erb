
<h2>
	<% if @user.nil? %>
		Your
	<% else %>
		<%= @user.username %>'s
	<% end %>
	Idols
</h2>

<% unless @user.nil? %>
	<%= link_to "All posts by their idols", idols_posts_user_path %>
<% end %>

<%= render "simplelist" %>