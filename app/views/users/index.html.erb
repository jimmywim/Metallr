<h2>All Users</h2>

<div class="row-fluid">
  <table id="users-table">
    <tr>
      <th>Username</th>
      <th>Email</th>
      <th></th>
      <th></th>
      <th></th>
    </tr>

  <% @users.each do |user| %>
    <tr>
      <td><%= user.username %></td>
      <td><%= user.email %></td>
      <td><%= link_to 'Show', user %></td>
      <td><%= link_to 'Edit', edit_user_path(user) %></td>
      <td><%= link_to 'Delete Forever', user, :method => :delete, :data => { :confirm => 'Are you sure?' }, :Class => "delete_user" %></td>
    </tr>
  <% end %>
  </table>
</div>

<%= link_to 'Create User Account', new_user_registration_path %>