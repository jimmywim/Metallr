
<h2>
	<% if @user.nil? %>
		<%= current_user.username %>
	<% else %>
		<%= @user.username %>
	<% end %>
	's Groupies
</h2>

<% unless @user.nil? %>
	<%= link_to "All posts by their groupies", groupies_posts_user_path %>
<% end %>

<%= render "simplelist" %>